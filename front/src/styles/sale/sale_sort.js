import styled from '@emotion/styled';

const DivSaleSort = styled.div`
  flex: 0 1 auto;
  margin: 0 100px;
  select {
    border: none;
    font-size: 14px;
    font-weight: 500;
  }
`;

export { DivSaleSort };
